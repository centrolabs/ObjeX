services:
  objex:
    build: .
    ports:
      - "9001:9001"
      - "9000:9000"
    volumes:
      - objex-data:/data
    environment:
      DefaultAdmin__Password: admin
    restart: unless-stopped

volumes:
  objex-data:
